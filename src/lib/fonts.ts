import { JetBrains_Mono as Mono, Inter as Sans } from "next/font/google"

export const sans = Sans({ subsets: ["latin"], variable: "--font-sans" })
export const mono = Mono({
  subsets: ["latin"],
  variable: "--font-mono",
})
